# Demo mode (default): no external LLM credentials required
DEMO_MODE=true

# Used only when DEMO_MODE=false
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# Path to the candidate data pack (official pack when provided)
DATA_PACK_PATH=./candidate-pack

PORT=3000
